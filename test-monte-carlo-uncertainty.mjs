import assert from 'node:assert/strict';import {binomialUncertainty,uncertaintyFields} from './monte-carlo-core.mjs';const a=binomialUncertainty(.5,10000);assert.ok(a.lower<.5&&a.upper>.5);assert.ok(a.halfWidth<.011);const b=uncertaintyFields(.02,30000);assert.ok(b.ci95.lower>0);assert.ok(b.ci95.upper>.02);assert.equal(binomialUncertainty(-1,10).se,null);console.log('monte carlo uncertainty passed');
