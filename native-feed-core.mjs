const TEAM_ALIAS={ARI:'AZ',OAK:'ATH'};
export const teamAlias=t=>TEAM_ALIAS[String(t||'').toUpperCase()]||String(t||'').toUpperCase();
export function isoFromSeasonStats(s={}){const avg=Number(s.avg),slg=Number(s.slg);if(!Number.isFinite(avg)||!Number.isFinite(slg))return null;return +(slg-avg).toFixed(3)}
function statusText(g={}){return g.status?.detailedState||g.status?.abstractGameState||'Scheduled'}
function probable(g,side){const p=g.teams?.[side]?.probablePitcher;return{name:p?.fullName||null,id:p?.id?+p.id:null}}
export function scheduleItems(schedule={}){const games=(schedule.dates||[]).flatMap(d=>d.games||[]);return games.map(g=>{const a=probable(g,'away'),h=probable(g,'home');return{gamePk:+g.gamePk,away:teamAlias(g.teams?.away?.team?.abbreviation),home:teamAlias(g.teams?.home?.team?.abbreviation),awayTeamId:+g.teams?.away?.team?.id||null,homeTeamId:+g.teams?.home?.team?.id||null,awayStarter:a.name,homeStarter:h.name,awayStarterId:a.id,homeStarterId:h.id,status:statusText(g),awayLineup:0,homeLineup:0}}).filter(g=>g.gamePk&&g.away&&g.home)}
function pitcherHand(feed,id){const p=feed.gameData?.players?.['ID'+id];return p?.pitchHand?.code||p?.pitchHand?.description||null}
function battingOrderPlayers(sideBox={},team,matchup,opp={}){const players=sideBox.players||{},order=Array.isArray(sideBox.battingOrder)?sideBox.battingOrder:[];return order.slice(0,9).map((id,i)=>{const p=players['ID'+id]||players[String(id)]||{},season=p.seasonStats?.batting||{};return{player:p.person?.fullName||null,player_id:+(p.person?.id||id),team:teamAlias(team),matchup,lineup:i+1,opp_pitcher:opp.name||null,opp_pitcher_id:opp.id||null,opp_id:opp.id||null,opp_pitcher_hand:opp.hand||null,bat_side:p.person?.batSide?.code||p.person?.batSide?.description||null,iso:isoFromSeasonStats(season)}}).filter(x=>x.player&&x.player_id)}
export function parseGameFeed(feed={},scheduleGame={}){const bs=feed.liveData?.boxscore?.teams||{},awayAbbr=teamAlias(bs.away?.team?.abbreviation||scheduleGame.away),homeAbbr=teamAlias(bs.home?.team?.abbreviation||scheduleGame.home),awayHand=scheduleGame.awayStarterId?pitcherHand(feed,scheduleGame.awayStarterId):null,homeHand=scheduleGame.homeStarterId?pitcherHand(feed,scheduleGame.homeStarterId):null,awayOpp={name:scheduleGame.homeStarter||null,id:scheduleGame.homeStarterId||null,hand:homeHand},homeOpp={name:scheduleGame.awayStarter||null,id:scheduleGame.awayStarterId||null,hand:awayHand},away=battingOrderPlayers(bs.away,awayAbbr,`${awayAbbr} @ ${homeAbbr}`,awayOpp),home=battingOrderPlayers(bs.home,homeAbbr,`${homeAbbr} @ ${awayAbbr}`,homeOpp);return{away,home,awayLineup:away.length>=9?9:away.length,homeLineup:home.length>=9?9:home.length,awayStarterHand:awayHand,homeStarterHand:homeHand,venue:feed.gameData?.venue?.name||null,weather:feed.gameData?.weather?{condition:feed.gameData.weather.condition||null,temp_f:feed.gameData.weather.temp??null,wind:feed.gameData.weather.wind||null}:null}}
async function getJSON(fetcher,url,timeoutMs=12000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetcher(url,{headers:{accept:'application/json','user-agent':'BANDALYTICS/95'},signal:c.signal});if(!r.ok)throw Error(`MLB HTTP ${r.status}`);return await r.json()}finally{clearTimeout(t)}}
export async function buildNativeFeed({date,fetcher=fetch,timeoutMs=12000}={}){if(!/^20\d\d-\d\d-\d\d$/.test(String(date||'')))throw Error('date required');const u=`https://statsapi.mlb.com/api/v1/schedule?sportId=1&date=${encodeURIComponent(date)}&hydrate=probablePitcher,team`,schedule=await getJSON(fetcher,u,timeoutMs),items=scheduleItems(schedule),lineup_players=[];await Promise.all(items.map(async g=>{try{const feed=await getJSON(fetcher,`https://statsapi.mlb.com/api/v1.1/game/${g.gamePk}/feed/live`,timeoutMs),q=parseGameFeed(feed,g);Object.assign(g,{awayLineup:q.awayLineup,homeLineup:q.homeLineup,awayStarterHand:q.awayStarterHand,homeStarterHand:q.homeStarterHand,venue:q.venue,weather:q.weather});lineup_players.push(...q.away,...q.home)}catch(e){g.lineup_error=e?.message||String(e)}}));const starters=items.reduce((n,g)=>n+(g.awayStarter?1:0)+(g.homeStarter?1:0),0),lineups=items.reduce((n,g)=>n+(g.awayLineup>=9?1:0)+(g.homeLineup>=9?1:0),0);return{ok:true,date,games:items.length,starter_slots:items.length*2,starters,lineup_slots:items.length*2,lineups,lineup_players,items,source:'MLB_STATSAPI_DIRECT_V2',model_scoring_changed:false}}
