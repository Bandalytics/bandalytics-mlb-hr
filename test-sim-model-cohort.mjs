import assert from 'node:assert/strict';import {modelCohorts,selectModelCohort} from './sim-model-cohort-core.mjs';const rows=[{modelFingerprint:'a',valid:true,date:'2026-08-01',protocolId:'P1'},{modelFingerprint:'b',valid:true,date:'2026-08-02',protocolId:'P2'},{modelFingerprint:'a',valid:false,date:'2026-08-03',protocolId:'P1'}];const c=modelCohorts(rows);assert.equal(c.length,2);assert.equal(c.find(x=>x.fingerprint==='a').records,2);assert.equal(c.find(x=>x.fingerprint==='a').valid,1);const s=selectModelCohort(rows,'b');assert.equal(s.records.length,1);assert.equal(s.explicit,true);console.log('model cohort partitioning passed');
