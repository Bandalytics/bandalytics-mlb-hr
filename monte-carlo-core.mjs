export function binomialUncertainty(p,n,z=1.96){p=+p;n=+n;if(!(p>=0&&p<=1)||!(n>0))return{se:null,lower:null,upper:null,halfWidth:null};const se=Math.sqrt(p*(1-p)/n),zz=z*z,den=1+zz/n,center=(p+zz/(2*n))/den,rad=z*Math.sqrt(p*(1-p)/n+zz/(4*n*n))/den,lower=Math.max(0,center-rad),upper=Math.min(1,center+rad);return{se,lower,upper,halfWidth:(upper-lower)/2}}
export function uncertaintyFields(p,n){const u=binomialUncertainty(p,n);return{se:u.se==null?null:+u.se.toFixed(6),ci95:u.lower==null?null:{lower:+u.lower.toFixed(6),upper:+u.upper.toFixed(6),halfWidth:+u.halfWidth.toFixed(6),lowerPct:+(100*u.lower).toFixed(3),upperPct:+(100*u.upper).toFixed(3)}}}
