import {buildSavantBbeUrl,summarizeBbeCsv} from '../bbe-core.mjs';
async function fetchText(url,timeoutMs=12000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0','accept':'text/csv,*/*'},signal:c.signal});if(!r.ok)throw Error('Baseball Savant BBE HTTP '+r.status);return await r.text()}finally{clearTimeout(t)}}
export default async function handler(req,res){try{const date=String(req.query?.date||''),ids=String(req.query?.ids||'').split(',').map(Number).filter(Number.isInteger).slice(0,300);const {url}=buildSavantBbeUrl({ids,date}),csv=await fetchText(url,12000),items=summarizeBbeCsv(csv,{ids,date});res.setHeader('Cache-Control','s-maxage=120, stale-while-revalidate=300');return res.status(200).json({ok:true,date,items,source:'BASEBALL_SAVANT_DIRECT',parity_candidate:true,scoring_cutover:false})}catch(e){return res.status(502).json({ok:false,error:e?.name==='AbortError'?'Baseball Savant timeout':(e?.message||String(e)),source:'BASEBALL_SAVANT_DIRECT',scoring_cutover:false})}}
