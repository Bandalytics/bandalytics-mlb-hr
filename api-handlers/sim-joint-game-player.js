import {simulateSlate} from '../sim-auto-core.mjs';
import {loadBaseContext,hydrateGameStats,hydrateGameStatsAsOf,hydrateHitters,hydrateHittersAsOf} from '../sim-data-core.mjs';
import {simulateJointGamePlayer} from '../sim-joint-game-player-core.mjs';
const norm=t=>String(t||'').toUpperCase()==='ARI'?'AZ':String(t||'').toUpperCase()==='OAK'?'ATH':String(t||'').toUpperCase();
const parkHr=(rows,venue)=>{const x=(rows||[]).find(r=>String(r.venue||'').toLowerCase()===String(venue||'').toLowerCase());if(!x)return 1;const c=+x.current?.hr_factor||100,r=+x.rolling3?.hr_factor||c;return Math.max(.70,Math.min(1.35,(.35*c+.65*r)/100))};
export default async function handler(req,res){try{if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({ok:false,error:'POST required'})}const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{}),date=String(body.date||''),gamePk=+body.gamePk,playerSelections=Array.isArray(body.playerSelections)?body.playerSelections:[],gameSelections=Array.isArray(body.gameSelections)?body.gameSelections:[],sims=Math.max(5000,Math.min(100000,+body.sims||30000)),historical=body.historical===true||String(body.historical||'0')==='1';if(!/^20\d\d-\d\d-\d\d$/.test(date)||!Number.isInteger(gamePk)||(!playerSelections.length&&!gameSelections.length))return res.status(400).json({ok:false,error:'date, gamePk and selections required'});const warnings=[],{feed,bullpen,park,weather}=await loadBaseContext(date),raw=(feed.items||[]).find(g=>+g.gamePk===gamePk);if(!raw)return res.status(404).json({ok:false,error:'game not found'});const gameStats=historical?await hydrateGameStatsAsOf(feed,date,warnings):await hydrateGameStats(feed,warnings),hitterHydration=historical?await hydrateHittersAsOf(feed,date,warnings):await hydrateHitters(feed,date,warnings),{pitcherStats,teamStats}=gameStats,{hitterStats,bbeStats}=hitterHydration,slate=simulateSlate({feed,bullpen,park,weather,pitcherStats,teamStats,sims:5000,includeLive:true,historicalSafe:historical}),sg=slate.games.find(g=>+g.gamePk===gamePk);if(!sg)return res.status(404).json({ok:false,error:'sim game unavailable'});const hrf=historical?1:parkHr(park.items,sg.venue),contexts=new Map();for(const p of (feed.lineup_players||[]).filter(p=>norm(p.team)===norm(sg.away)||norm(p.team)===norm(sg.home))){const isAway=norm(p.team)===norm(sg.away);contexts.set(+p.player_id,{player:p,stats:hitterStats.get(+p.player_id),bbe:bbeStats.get(+p.player_id)||null,teamExpectedRuns:isAway?sg.estimates.away.expectedRuns:sg.estimates.home.expectedRuns,starter:pitcherStats.get(+(isAway?raw.homeStarterId:raw.awayStarterId))||{},parkHrFactor:hrf})}const pushable=new Set(['game_over','game_under','home_tt_over','home_tt_under','away_tt_over','away_tt_under']);for(const x of gameSelections)if(pushable.has(String(x.market||''))&&Number.isInteger(+x.line))return res.status(400).json({ok:false,error:'Joint total/team-total legs must use half-run lines (.5) so push outcomes are not mispriced as losses.'});const result=simulateJointGamePlayer({away:sg.away,home:sg.home,awayExpectedRuns:sg.estimates.away.expectedRuns,homeExpectedRuns:sg.estimates.home.expectedRuns,playerSelections,gameSelections,playerContexts:contexts,sims});return res.status(result.ok?200:400).json({...result,date,gamePk,historicalMode:historical,asOfDate:historical?date:null,statsCutoffDate:historical?(gameStats.statsCutoffDate||hitterHydration.statsCutoffDate||null):null,strictHistoricalInputs:historical,warnings,warning:'Game and player legs share the same simulated offensive environment. This is research-only and not calibrated for sportsbook SGP pricing.'})}catch(e){return res.status(500).json({ok:false,error:String(e?.message||e)})}}
