import {walkForwardEvaluation} from '../sim-joint-walkforward-core.mjs';export default async function handler(req,res){try{if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({ok:false,error:'POST required'})}const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{}),records=Array.isArray(b.records)?b.records:[];if(!records.length)return res.status(400).json({ok:false,error:'records required'});const evaluation=walkForwardEvaluation(records,{minTrainDays:b.minTrainDays,testDays:b.testDays,stepDays:b.stepDays});return res.status(200).json({ok:true,evaluation,researchOnly:!evaluation.readyForSportsbookSgpEv})}catch(e){return res.status(500).json({ok:false,error:String(e?.message||e)})}}
