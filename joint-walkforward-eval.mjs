#!/usr/bin/env node
import fs from 'node:fs/promises';import {walkForwardEvaluation} from './sim-joint-walkforward-core.mjs';const arg=(k,d=null)=>{const i=process.argv.indexOf(k);return i>=0?process.argv[i+1]:d},input=arg('--in'),out=arg('--out','joint-walkforward-report.json');if(!input){console.error('usage: node joint-walkforward-eval.mjs --in replay-batch.json [--min-train-days 30] [--test-days 7] [--step-days 7] [--out report.json]');process.exit(2)}const data=JSON.parse(await fs.readFile(input,'utf8')),records=Array.isArray(data.records)?data.records:Array.isArray(data)?data:[],evaluation=walkForwardEvaluation(records,{minTrainDays:+arg('--min-train-days','30'),testDays:+arg('--test-days','7'),stepDays:+arg('--step-days',arg('--test-days','7'))});await fs.writeFile(out,JSON.stringify({generatedAt:new Date().toISOString(),source:input,evaluation},null,2));console.log(JSON.stringify({out,folds:evaluation.folds.length,oosRecords:evaluation.oosRecords,checks:evaluation.checks,ready:evaluation.readyForSportsbookSgpEv},null,2));
